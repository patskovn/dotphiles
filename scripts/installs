brew tap homebrew/cask-fonts && brew install --cask font-fira-code-nerd-font
brew install tmux
brew install fish

git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
echo "Don't forget to install plugins via prefix-I and prefix-U"
